<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="utf-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1">
    <!-- The above 3 meta tags *must* come first in the head; any other head content must come *after* these tags -->
    <meta name="description" content="">
    <meta name="author" content="">
    <link rel="icon" href="../../favicon.ico">

    <title><%= title %></title>
    <%# csrf_meta_tags %>
    <%= css_url %w(bootstrap.min.css) %>
    <%= yield_content :css %>
    <%= css_url %w(style.css) %>
  </head>

  <body>
    <%= erb :'partials/_menu' %>

    <!-- Begin page content -->
    <div class="container">
      <%= yield %>
    </div>

    <footer class="footer">
      <div class="container">
        <p class="text-muted">&copy; <%= Time.now.year %> <%= project_name %></p>
      </div>
    </footer>

    <!-- Placed at the end of the document so the pages load faster -->
    <%= js_url %w(jquery-3.1.1.min.js bootstrap.min.js) %>
    <%= yield_content :js %>
  </body>
</html>